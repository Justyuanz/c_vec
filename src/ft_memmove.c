#include "vec.h"

void *memmove(void *dst, const void *src, size_t n)
{
	size_t	i;
	unsigned char	*dest;
	const void	*source;

	dest = (unsigned char *)dst;
	source = (const unsigned char *)src;
	i =  0;
	if (dest <= source)
	{
		while()
	}
	else
	{

	}
}